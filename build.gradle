plugins {
    id "application"
    id 'org.openjfx.javafxplugin' version '0.0.8'
}
javafx {
    version = "11.0.2"
    modules = [ 'javafx.controls', 'javafx.fxml' ]
}
repositories {
    mavenCentral()
 }
 dependencies {
    compile group: 'commons-validator', name: 'commons-validator', version: '1.6'
    compile group: 'com.google.code.gson', name: 'gson', version: '2.7'
}
apply plugin: 'java'


task(runUI, dependsOn: 'classes', type: JavaExec){
    doFirst {
        jvmArgs = ['--module-path', 'E:/JavaJDK/javafx-sdk-11.0.2/lib', '--add-modules', 'javafx.controls,javafx.fxml']
    }
    main = "main.java.sample.StartUi"
    classpath = sourceSets.main.runtimeClasspath
}

defaultTasks 'runUI'


//mainClassName = ["main.java.sample.StartServer", "main.java.sample.StartUi"]

